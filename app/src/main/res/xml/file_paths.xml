<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Access files in the app's cache directory -->
    <cache-path
        name="pdf_cache"
        path="." />

    <!-- Access files in the app's files directory -->
    <files-path
        name="pdf_files"
        path="." />

    <!-- If saving to external storage (optional) -->
    <external-path
        name="external_pdf"
        path="Download/" />
</paths>
